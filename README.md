# PyQtPopupDemo


This is a demo Python program of a macOS menu bar extra app that opens a popup.  Although it uses the PySide6 framework, it will also work with PyQt6.

This demo app illustrates a workaround for the sporadic issue of the popup window sometimes not being focused when invoked by its menu bar extra icon, causing mouse clicks within it to be ignored or "missed."

